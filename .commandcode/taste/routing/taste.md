- Prefers nuqs (useQueryStates) over react-router's useSearchParams for URL query parameter management. nuqs v2 with parsers like parseAsInteger, parseAsString should be used; NuqsAdapter should wrap the app root. Confidence: 0.70
- When defining nuqs parsers, use typed parsers that preserve the original TypeScript type — `parseAsInteger` for numbers and `parseAsStringEnum` for enums — so that downstream code uses native types without manual `Number()` or `as` casts. Confidence: 0.75
- For query parameters that are string unions (e.g., `keyof` types like `TUserSortBy`) that aren't TypeScript enums, use nuqs `createParser` to build a custom parser that validates against the allowed set of values and logs `console.error` for any invalid value found in the URL. Confidence: 0.70
- When URL query params use snake_case keys (e.g., `page_size`, `sort_by`) but JS code expects camelCase, wrap nuqs `useQueryStates`/`useQueryState` in a reusable custom hook that auto-derives the `urlKeys` mapping (camelCase parser keys → snake_case URL params via a `toSnakeCase` converter). This eliminates per-page `useMemo` boilerplate that manually redeclares every field — callers receive camelCase state directly, the URL stays snake_case, and nuqs' built-in `urlKeys` option does the heavy lifting. Confidence: 0.80
